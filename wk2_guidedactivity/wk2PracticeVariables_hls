<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="css/style.css">
    <title>Wk2 Guided Activity</title>
  </head>
  
  <body>

<script>

let firstName = 'Taylor';
console.log(firstName);

// declare more variables

let lastName = 'Swift';
console.log(lastName);

let age = 34;
console.log(age);

// what is the result of following code?

let job;
console.log(job);

job = 'Singer';
console.log(job);

// concatenate values

console.log(firstName + ' ' + lastName)

// alerts

alert(firstName+ ' is a ' + job);

// prompts
let favFood = prompt('What is your favorite food?');
console.log('Your favorite food is: ' + favFood);

// create a constant variable named entree and set it to equal the string 'Chicken Tenders'
const entree = 'Chicken Tenders';

// create another constant variable named price that is equal to set price
const price = 10;

// print values stored in the variables to console

console.log(entree);
console.log(price);

// what will happen when you type this line? entree = 'Fettuccine Alfredo'

let meal = 'Chicken Tenders';
console.log(meal);
meal = 'Fettuccine Alfredo';
console.log(meal);

// use const if value doesn't change within the program

const fName = 'Travis';
const lName = 'Kelce';
const ddAge = 34;

console.log(fName + ' ' + lName + ' is ' + ddAge + ' years old!' );

let x = 5;
x = x + 1;

</script>
</body>
</html>
